# Migration `watch-20191219005421`

This migration has been generated by Medet Tleukabiluly ✎ at 12/19/2019, 12:54:22 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "lift"."User" ADD COLUMN "birthdate" DATE    ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191219003515..watch-20191219005421
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 enum Status {
   FRIENDS_WITH_BENEFITS
@@ -37,8 +37,9 @@
   id              String @default(cuid()) @id
   phone           String @unique
   name            String
   password        String
+  birthdate       DateTime?
   deviceId        String?
   country         String?
   city            String?
   images          File[]
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191219005421)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191219005421'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
